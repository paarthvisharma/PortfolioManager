package controller;

/**
 * The Controller Interface represents the Controller part of the Stocks Program out of the three
 * broad categories of model-view-controller (MVC) design.
 * This Controller Interface takes inputs from the user and
 * delegates responsibility - tells the model what to do and the view what to show.
 */
public interface Controller {
  /**
   * Method to set up the menu options in order to take user inputs.
   *
   * @throws InterruptedException when * symbol is entered.
   */

  void mainMenuLoop() throws InterruptedException;

  /**
   * Method to run the loop to create a new user.
   *
   * @throws InterruptedException when * symbol is entered.
   */
  void createUserLoop() throws InterruptedException;

  /**
   * Method to run the loop to create portfolio for users.
   *
   * @throws InterruptedException when * symbol is entered.
   */
  void createPortfolioLoop() throws InterruptedException;

  /**
   * Method to run the loop to display the created portfolios associated with a user.
   *
   * @throws InterruptedException when * symbol is entered.
   */
  void viewPortfolioLoop() throws InterruptedException;

  /**
   * Method to run the loop to calculate the value of different portfolios associated with a user.
   *
   * @throws InterruptedException when * symbol is entered.
   */

  void valuePortfolioLoop() throws InterruptedException;

  /**
   * Method to run the loop to load the user information from already
   * present XML file to thr program.
   *
   * @throws InterruptedException when * symbol is entered.
   */
  void loadUserFromXmlLoop() throws InterruptedException;

  /**
   * Method to continue displaying the main menu options.
   */
  void runLoop();
}
